Usage: goam [OPTIONS] install

Description:
  Builds and installs files provided by the project, as specified by
  the "GOAM.conf" configuration files of the project.

Command chain:
  goam install <-- goam make <-- goam install-deps
